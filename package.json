{
  "name": "deploy.mvp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "webpack --mode=development --watch",
    "build:prod": "webpack --mode=production",
    "format": "prettier --check server/ src/ webpack.config.js .github/ || echo '⚠️  Code style issues found. Run `pnpm run format:fix` to fix them.'",
    "format:fix": "prettier --write server/ src/ webpack.config.js *.yml",
    "lint": "npx eslint 'server/**/*.js' 'src/**/*.js'",
    "lint:fix": "npx eslint --fix 'server/**/*.js' 'src/**/*.js'",
    "server": "nodemon ./index.js -w server -w index.js --ignore src --ignore dist",
    "start": "npm run build & npm run server",
    "test": "echo 'need test'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "eslint": "^9.12.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react": "^7.37.1",
    "globals": "^15.10.0",
    "html-webpack-plugin": "^5.6.0",
    "nodemon": "^3.1.7",
    "prettier": "^3.3.3",
    "webpack": "^5.95.0",
    "webpack-cli": "^5.1.4",
    "webpack-obfuscator": "^3.5.1"
  },
  "pnpm": {
    "overrides": {
      "cookie@<0.7.0": ">=0.7.0"
    }
  }
}
